#include <stdio.h>
#include "main.h"

/**
 * mul - thus function takes in two integers and
 * multiply them
 * @a: parameter
 * @b: parameter
 * Return: returns the result od the multiplication
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
